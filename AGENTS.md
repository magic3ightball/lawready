# AGENTS.md

이 파일은 **저장소에서 작업할 때 필요한 운영 정보**만 담습니다. 제품·기능 내용의 권위는 원본 문서에 있으며, 여기에 옮겨 적지 않습니다. 무엇을 알아야 하면 아래 표에서 해당 문서를 찾아 직접 읽으십시오.

## 1. 지금 저장소 상태

- **실행 코드가 없습니다.** `apps/web/`, `apps/api/`, `packages/`, `tests/`에는 각각 README 스텁 한 개만 있습니다.
- `package.json`, 빌드 도구, 테스트 러너, 린터, `.github/workflows/`가 **모두 없습니다.** 실행할 테스트나 빌드 명령이 존재하지 않습니다.
- 따라서 현재 들어오는 작업은 사실상 전부 문서 작업입니다. 요청받지 않은 스택·도구를 임의로 도입하지 마십시오.

## 2. 무엇을 확인할 때 어떤 파일을 여는가

파일별 **용도**는 README `문서 구성` 표를 보십시오. 아래 표는 그 표에 없는 **장 단위 위치**를 더합니다.

| 확인할 것 | 파일 | 위치 |
|---|---|---|
| 제품이 무엇이고 누구를 위한 것인가 | `docs/product/서비스_소개서.md` | 01~07장 |
| 변경 대응·출시 게이트·미확정 항목 | `docs/product/서비스_소개서.md` | 문서 끝 `상세 원칙` |
| 완료의 정의와 7단계 사용자 여정 | `docs/product/기능정의서.docx` | 1장 `MVP 한눈에 보기`(본문 제목은 `MVP 개요`) |
| 이 기능을 만들어야 하나 · 완료조건 | `docs/product/기능정의서.docx` | 2장 `필수 구현 기능 정의` |
| 발행 차단 규칙 · 공통 비기능 수용 기준 | `docs/product/기능정의서.docx` | 3장 `공통 검증과 발행 차단` |
| 재량으로 구현해도 되나 | `docs/product/기능정의서.docx` | 4장 `재량 구현·현재 구현 제외` 중 재량 구현 |
| 구현하면 안 되나 | `docs/product/기능정의서.docx` | 4장 `재량 구현·현재 구현 제외` 중 현재 구현 제외(후속 검토 후보 / 선결정 필요) |
| 수용 시나리오 · 테스트 fixture | `docs/product/기능정의서.docx` | 5장 `핵심 수용 시나리오` |
| 절대 어기면 안 되는 제품 제약 | `docs/product/기능정의서.docx` `최상위 원칙` + 3장 VAL-01~09 | 평문 요약은 `서비스_소개서.md` 02장 `중요` · 04장 `발행 원칙` |
| 역할·도메인 객체·상태 모델·비기능 | `docs/architecture/기능정의서_기술검토_부록.docx` | 1~4장 |
| 기술 선택이 정해졌나 | `docs/architecture/기능정의서_기술검토_부록.docx` | 4.1 `MVP 기술 선택 상태` |
| 오픈소스 조사 · 착수 게이트 · 기존 68개 ID 매핑 | `docs/architecture/기능정의서_기술검토_부록.docx` | 6~8장 |
| 왜 이렇게 정해졌나 (권위 없음) | `docs/records/서비스_기획_대화_기록_2026-08-25.md` | — |
| 무엇이 언제 바뀌었나 | `CHANGELOG.md` | — |

위 장 번호는 아래 §3에 적힌 문서 버전 기준입니다. 버전이 다르면 다시 추출해 확인하십시오.

**문서 간 우선순위 (원문에 명시된 것만):**

- 범위·완료조건이 충돌하면 **기능정의서를 우선**합니다. (부록 `사용 원칙`, 부록 5장)
- 부록 `읽는 법`: "기능 우선순위나 제품 범위를 이 부록만으로 확대하지 않습니다." (README `문서 구성` 마지막 문단에도 같은 원칙이 있습니다.)
- 단, **법률 콘텐츠 적정성·공식 사이트 수집 허용·AI 공급업체·데이터 보유정책**은 기능정의서 `문서 경계`가 **부록의 착수 게이트를 확인하라고 지시**합니다.
- 대화 기록은 배경 자료이며 확정 범위가 아닙니다. (문서 자체 명시)
- 소개서와 부록 사이의 우열은 어느 문서에도 정의되어 있지 않습니다. 임의로 판단하지 말고 사람에게 확인하십시오.

## 3. `.docx` 사양서 다루기

두 사양서는 **바이너리**입니다(`.gitattributes`: `*.docx binary`). `grep`·`diff`·직접 읽기가 되지 않으며, 변경 내용을 리뷰로 확인할 수 없습니다.

읽을 때는 아래를 그대로 사용하십시오. 표의 셀 구분을 살려야 기능표를 읽을 수 있습니다.

```bash
python3 -c "
import zipfile, re, sys
x = zipfile.ZipFile(sys.argv[1]).read('word/document.xml').decode('utf-8')
x = x.replace('</w:tr>', '\n---\n').replace('</w:p>', '\n').replace('</w:tc>', ' | ')
print(re.sub(r'<[^>]+>', '', x))
" "docs/product/기능정의서.docx"
```

- **장 찾는 법은 두 문서가 서로 반대입니다.**
  - **기능정의서**: 본문 제목에 장 번호가 없습니다(번호는 `word/numbering.xml`에 있어 추출 결과에 나타나지 않음). 추출 결과 앞부분의 **목차(TOC) 블록**으로 번호와 제목을 대응시키십시오. `"3장"`이나 `"3."`으로 검색하면 찾지 못합니다.
  - **부록**: 반대로 본문 제목에 번호가 그대로 붙어 있고(`1. 역할과 권한 경계` … `8. 기존 기능 ID 처리표`) **TOC 블록이 없습니다.** `grep -nE '^[0-9]+(\.[0-9]+)*\.? '`로 바로 장을 찾으십시오. 표 안의 번호 항목도 함께 걸리지만 제목과 쉽게 구분됩니다.
- 문서 버전은 추출 결과 앞부분에 있습니다. 작성 시점 기준 기능정의서 `v2.9 · 2026-08-25`, 부록 `v1.14 · 2026-08-25`입니다. **버전이 다르면** 이 파일의 장 구성·ID 목록이 낡았을 수 있으니 다시 추출해 확인하십시오.
- **각주는 추출되지 않습니다.** 위 명령은 `word/document.xml`만 읽습니다. 기능정의서의 각주(`word/footnotes.xml`)에는 국가별 법령 출처 5건이 들어 있으니 필요하면 따로 읽으십시오. 같은 목록이 부록 `1.2 SET-04 공식 검토 출처`에 본문으로도 있습니다. 부록에는 각주가 없습니다.
- `.docx`를 텍스트 도구로 편집하지 마십시오. 내용 수정이 필요하면 사람에게 돌려주십시오.
- `docs/architecture/lawready-system-architecture.png`도 같은 이유로 diff에서 내용을 확인할 수 없습니다. 이미지로 직접 열어 보십시오.
- `~$*.docx` (Word 잠금 파일)는 이미 gitignore되어 있습니다.

## 4. 식별자 사전

내용은 담지 않습니다. 이름만 알아두고 원문을 여십시오. 이 목록도 §3의 문서 버전 기준입니다.

- **필수 구현 기능 26개** (기능정의서 2장): `SET-01~04` · `INP-01~04` · `FAC-01~03` · `DOC-01~05` · `PUB-01~06` · `OPS-01~04`
- **공통 검증** (3장): `VAL-01~09`
- **수용 테스트 fixture** (5장): `SAFE-01` · `PII-01` · `SECRET-01` · `MALWARE-01` · `CONFLICT-01` · `RISK-01` · `SOURCE-FAIL-01` · `SOURCE-CHANGE-01` · `APPROVAL-01`
- **기존 68개 ID → v2.0 매핑**: 부록 8장

> `CHANGELOG.md`의 과거 항목에는 `FAC-01~05`, `28개 기능` 같은 옛 표기가 남아 있습니다. 현재 기준은 위 목록입니다.

## 5. 저장소 고유 작업 규칙

브랜치·PR·작업 위치 규칙은 [`.github/CONTRIBUTING.md`](.github/CONTRIBUTING.md)를, 변경 관리 원칙과 문서의 목적·용도는 [`README.md`](README.md)를 따릅니다. 여기에는 그 문서들에 없는 것만 적습니다.

- **`CHANGELOG.md` 삽입 위치**: 해당 날짜 절의 **맨 위**에 넣고, 커밋 단위로 빈 줄로 구분된 묶음을 만듭니다. 절 아래에 덧붙이지 마십시오. (맨 위 삽입은 기존 19개 커밋 전부가 이 방식입니다. 빈 줄 구분은 최근 6개 커밋의 방식이며 그 이전에는 섞여 있습니다.)
- **`git add -A`를 쓰지 마십시오.** 경로를 명시해 스테이징합니다. `.omc/`는 에이전트 도구가 만든 디렉터리로 **추적 대상이 아니고 `.gitignore`에도 없어서** `-A`에 그대로 딸려 들어갑니다. 이 저장소에 커밋하지 말고, `.gitignore`에 추가하지도 마십시오 — 개인 도구 디렉터리를 공용 저장소의 `.gitignore`에 넣지 않습니다. 로컬에서 숨기려면 `.git/info/exclude`를 쓰십시오.
- **커밋 제목**: README의 예시는 한국어지만 실제 히스토리는 **영문 제목**입니다(23개 중 22개). 최근 15개는 `docs:`처럼 scope 없는 형태이고, 그 이전 8개 중 6개가 `docs(intro):`·`docs(spec):`처럼 scope를 붙였습니다. 주변 커밋에 맞추십시오.
- **한국어 파일 경로**: 거의 모든 문서 경로에 한글이 들어 있습니다. 셸 명령에서 경로를 **항상 따옴표로 감싸십시오.** 또한 `git ls-files` 등은 기본적으로 8진 이스케이프(`\352\270\260...`)로 출력하므로, 경로를 그대로 보려면 `git -c core.quotePath=false ...`를 쓰십시오.

## 6. 변경 후 확인

실행 가능한 테스트·빌드·린터가 없습니다. 대신 다음을 확인합니다.

1. `git status --porcelain` — 의도한 경로만 스테이징되었고 `.omc/`가 포함되지 않았는가
2. `git diff --cached --stat` — 의도하지 않은 `.docx`·`.png` 바이너리가 섞여 들어가지 않았는가
3. 문서에 적은 상대 링크가 실제로 존재하는가
4. `CHANGELOG.md` 항목을 해당 날짜 절 맨 위에 넣었는가
5. 새로 쓴 문서가 한국어이고 UTF-8인가

## 7. 이 파일이 다루지 않는 것

- 저장소 구조·문서의 목적과 용도·변경 관리 원칙·커밋 메시지 예시 → [`README.md`](README.md)
- 브랜치 이름·작업 위치·PR 규칙 → [`.github/CONTRIBUTING.md`](.github/CONTRIBUTING.md)
- 제품·기능·법률 요구사항의 실제 내용 → `docs/`

이 파일을 늘릴 때는 **위 문서에 이미 있는 내용을 옮겨 적지 마십시오.** 새로 적을 값이 있는 것은 작업 트리에서 관찰되지만 어느 문서에도 적혀 있지 않은 사실뿐입니다.
